<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
            line-height: 1.6;
        }

        .container {
            max-width: 600px;
            margin: 40px auto;
            background-color: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
            overflow: hidden;
        }

        .header {
            text-align: center;
            padding: 40px 20px;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: #ffffff;
        }

        .header h1 {
            margin: 0;
            font-size: 28px;
            font-weight: 600;
        }

        .header .icon {
            font-size: 48px;
            margin-bottom: 16px;
        }

        .header .order-number {
            font-size: 14px;
            opacity: 0.9;
            margin-top: 8px;
        }

        .content {
            padding: 40px 30px;
            color: #374151;
        }

        .content p {
            margin: 0 0 16px;
            font-size: 16px;
        }

        .greeting {
            font-size: 18px;
            font-weight: 500;
            color: #1f2937;
        }

        .order-details {
            background-color: #f9fafb;
            border-radius: 8px;
            padding: 24px;
            margin: 24px 0;
        }

        .order-details h3 {
            margin: 0 0 16px;
            font-size: 18px;
            color: #1f2937;
            border-bottom: 2px solid #e5e7eb;
            padding-bottom: 12px;
        }

        .order-item {
            display: flex;
            justify-content: space-between;
            padding: 12px 0;
            border-bottom: 1px solid #e5e7eb;
        }

        .order-item:last-child {
            border-bottom: none;
        }

        .order-item .item-name {
            font-weight: 500;
            color: #374151;
        }

        .order-item .item-price {
            color: #059669;
            font-weight: 600;
        }

        .order-total {
            display: flex;
            justify-content: space-between;
            padding: 16px 0 0;
            margin-top: 16px;
            border-top: 2px solid #10b981;
        }

        .order-total .label {
            font-size: 18px;
            font-weight: 600;
            color: #1f2937;
        }

        .order-total .amount {
            font-size: 20px;
            font-weight: 700;
            color: #059669;
        }

        .status-badge {
            display: inline-block;
            padding: 8px 16px;
            background-color: #d1fae5;
            color: #065f46;
            border-radius: 20px;
            font-size: 14px;
            font-weight: 600;
            margin: 16px 0;
        }

        .info-box {
            background-color: #eff6ff;
            border-left: 4px solid #3b82f6;
            padding: 16px;
            border-radius: 0 8px 8px 0;
            margin: 24px 0;
        }

        .info-box p {
            margin: 0;
            font-size: 14px;
            color: #1e40af;
        }

        .button-container {
            text-align: center;
            margin: 32px 0;
        }

        .view-order-button {
            display: inline-block;
            padding: 16px 48px;
            background: linear-gradient(135deg, #10b981 0%, #059669 100%);
            color: #ffffff !important;
            text-decoration: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
        }

        .delivery-info {
            background-color: #fef3c7;
            border-radius: 8px;
            padding: 20px;
            margin: 24px 0;
        }

        .delivery-info h4 {
            margin: 0 0 12px;
            color: #92400e;
            font-size: 16px;
        }

        .delivery-info p {
            margin: 0;
            font-size: 14px;
            color: #78350f;
        }

        .footer {
            text-align: center;
            padding: 24px 30px;
            background-color: #f9fafb;
            border-top: 1px solid #e5e7eb;
        }

        .footer p {
            margin: 0 0 8px;
            font-size: 13px;
            color: #9ca3af;
        }

        .footer .brand {
            font-weight: 600;
            color: #10b981;
        }

        .help-section {
            margin-top: 24px;
            padding-top: 24px;
            border-top: 1px solid #e5e7eb;
        }

        .help-section p {
            font-size: 14px;
            color: #6b7280;
        }

        .help-section a {
            color: #10b981;
            text-decoration: none;
            font-weight: 500;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="icon">✅</div>
            <h1>Order Confirmed!</h1>
            <% if (locals.orderId) { %>
                <p class="order-number">Order #<%= orderId %>
                </p>
                <% } %>
        </div>
        <div class="content">
            <p class="greeting">Hi<% if (locals.buyerUsername || locals.userName) { %>
                    <%= buyerUsername || userName %>
                        <% } %>,</p>
            <p>Great news! Your order has been confirmed and is being processed. Thank you for choosing
                <strong>Jobber</strong>!</p>

            <span class="status-badge">🎉 Order Confirmed</span>

            <div class="order-details">
                <h3>📋 Order Summary</h3>

                <% if (locals.serviceName || locals.gigTitle) { %>
                    <div class="order-item">
                        <span class="item-name">
                            <%= serviceName || gigTitle %>
                        </span>
                        <% if (locals.price || locals.amount) { %>
                            <span class="item-price">$<%= price || amount %></span>
                            <% } %>
                    </div>
                    <% } %>

                        <% if (locals.quantity) { %>
                            <div class="order-item">
                                <span class="item-name">Quantity</span>
                                <span>
                                    <%= quantity %>
                                </span>
                            </div>
                            <% } %>

                                <% if (locals.deliveryDays) { %>
                                    <div class="order-item">
                                        <span class="item-name">Delivery Time</span>
                                        <span>
                                            <%= deliveryDays %> day(s)
                                        </span>
                                    </div>
                                    <% } %>

                                        <% if (locals.total || locals.price || locals.amount) { %>
                                            <div class="order-total">
                                                <span class="label">Total</span>
                                                <span class="amount">$<%= total || price || amount %></span>
                                            </div>
                                            <% } %>
            </div>

            <% if (locals.sellerUsername) { %>
                <div class="info-box">
                    <p><strong>👤 Seller:</strong>
                        <%= sellerUsername %> will start working on your order soon. You'll receive updates as progress
                            is made.
                    </p>
                </div>
                <% } %>

                    <% if (locals.expectedDelivery || locals.deliveryDate) { %>
                        <div class="delivery-info">
                            <h4>📅 Expected Delivery</h4>
                            <p>
                                <%= expectedDelivery || deliveryDate %>
                            </p>
                        </div>
                        <% } %>

                            <% if (locals.orderLink) { %>
                                <div class="button-container">
                                    <a href="<%= orderLink %>" class="view-order-button">View Order Details</a>
                                </div>
                                <% } %>

                                    <div class="help-section">
                                        <p>Have questions about your order? <a href="#">Contact Support</a> or visit our
                                            <a href="#">Help Center</a>.</p>
                                    </div>

                                    <p>Thank you for your business!</p>
                                    <p>Best regards,<br><strong>The Jobber Team</strong></p>
        </div>
        <div class="footer">
            <p>© <%= new Date().getFullYear() %> <span class="brand">Jobber</span>. All rights reserved.</p>
            <p>This email was sent regarding your recent order on our platform.</p>
        </div>
    </div>
</body>

</html>